#include "main.h"
#include <stdio.h>

/**
 * _strcmp - entry
 * @s1: first
 * @s2: second
 * Return: difference
 */

int _strcmp(char *s1, char *s2)
{
	while (*s1 && *s2 && *s1 == *s2)
	{
		s1++;
		a2++
	}
	return (*s1 - *s2);
}
